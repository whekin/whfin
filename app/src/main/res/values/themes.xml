<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Whfin" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowBackground">@color/launch_background</item>
    </style>
    <style name="Theme.Whfin.QuickEntry" parent="Theme.Whfin">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- ModalBottomSheet owns the animated scrim. A second window-level dim would remain
             until Activity.finish(), making the launcher wake up one beat after the sheet. -->
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:colorAccent">#78906F</item>
    </style>
</resources>
